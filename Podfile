# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'CocoaPodsTool' do
  # Comment the next line if you don't want to use dynamic frameworks
  # use_frameworks!

  # Pods for CocoaPodsTool
  pod 'CommandLine'
  pod 'MUFoundation/MUPath'
  pod 'PodfileLock', :git => 'https://gitee.com/pica/PodfileLock.git', :branch => 'main'

end
